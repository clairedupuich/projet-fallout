window.onresize=function(){  
    changeDivHeight();  
 }  
 function changeDivHeight(){ 
       //宽度              
     var ChiWidth=$(document).width() < $('body').width() ? $(document).width() : $('body').width(); 
     if(ChiWidth<1000){
       //小于1000
      }else{
       //大于1000
      }
 
 
}